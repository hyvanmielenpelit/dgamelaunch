## $SERVERID

$ATTR(14)$GAMENAME Selected$ATTR()

The Curses interface default settings differ depending on your SSH client.

Next, select your SSH client:

p) PuTTY on Windows
s) ssh command in Terminal (Linux, macOS, Windows)
q) Return

=>
